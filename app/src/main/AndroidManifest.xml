<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rrp.duetlibrary">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DUETLibrary">
        <activity android:name=".AdminBookReturn"></activity>
        <activity android:name=".AdminBookIssues" />
        <activity android:name=".AdminAddBooks" />
        <activity android:name=".AdminAddStudent" />
        <activity android:name=".AdminProfile" />
        <activity android:name=".UserNotificationActivity" />
        <activity android:name=".UserBookSearchActivity" />
        <activity android:name=".UserProfileActivity" />
        <activity android:name=".UserDashboardActivity" />
        <activity android:name=".AdminDashboardActivity" />
        <activity android:name=".StudentDashboardActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>